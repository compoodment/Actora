Live Issues / Revisit Tracker
Date: 2026-03-29
Purpose: repo-local parking lot for current bugs, regressions, and revisit items so they do not get lost between patches.

- skip-time Esc/back UX lag concern
  Status: open
  Note: visible back guidance now favors `B`, but real-terminal feel around leaving skip-time should keep being watched during smoke passes.

- lineage search exit behavior
  Status: resolved in v0.36.7
  Note: `Esc` now exits lineage search mode first instead of immediately also acting like a full lineage back.

- lineage row overstuffing
  Status: resolved in v0.36.7
  Note: lineage rows were reduced to name, relationship, alive/dead, and age.

- lineage identity formatting confusion
  Status: resolved in v0.36.7
  Note: lineage detail now uses an explicit `Identity` block with labeled `Species`, `Sex`, and `Gender` lines.

- Life View event visibility regression
  Status: resolved in v0.36.7
  Note: the main ordinary-play TUI now keeps recent activity visible in the right pane.

- Life View content cutoff / scrollability need
  Status: partially resolved in v0.36.7
  Note: simple left-pane vertical scrolling exists now; fancier tabs/section systems remain later if still justified.

- planning-doc simplification / possible merge of actionable-summary + working-ideas-register later
  Status: open revisit
  Note: current planning docs still overlap more than ideal and may want a later simplification pass once the current TUI cleanup churn settles.

- old-age mortality still missing as real simulation truth
  Status: resolved in v0.36.8
  Note: baseline old-age mortality now resolves in simulation via world-owned monthly checks and surfaces through ordinary death/lineage truth.

- continuation substrate too narrow around startup-only parents
  Status: resolved in v0.36.9
  Note: startup can now include older siblings, later sibling births can occur through narrow family-event simulation, and siblings now surface directly in lineage/continuation truth.

- sibling/family birth tuning still needs later balancing
  Status: open revisit
  Note: the new age/spacing/family-size fertility heuristic is intentionally simple first-pass logic and may need probability tuning once more long-run play data exists.

- event history display is truncated and non-scrollable
  Status: open
  Note: the right pane currently shows only a few recent events. Next patch should replace this with an accumulating live feed (compact, year headers, skip markers, auto-scroll to latest) plus a dedicated full-screen scrollable history browser via `[H]`.
